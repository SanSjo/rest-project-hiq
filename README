## REST Project in Node.js with TDD

The project includes REST routes GET and POST the write anf get greetings. Including tests in jest.

## Usage
Install dependencies with:
```
npm install
```

Then run the project with:
```
node app.js
```
